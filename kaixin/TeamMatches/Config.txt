# 开心网外挂 配置文件 Config.txt
# "#"在行首表示该行是注释行

# 设置每次链接的延时(秒), 默认是5秒
# 如果遇到开心网服务器拒绝你的频繁，请适当加大这个数值
DelayConnect=1

# 设置每次链接的超时(秒)，默认是120秒
HttpTimeout=200

# 设置每次HTTP请求失败后尝试的次数，默认是3次
HttpTryTimes=3

# 代理服务器地址
#ProxyServer=server

# 代理服务器端口
#ProxyPort=80

# 代理服务器用户
#ProxyUser=abc

# 代理服务器用户密码
#ProxyPass=123

# 是否将log写入文件 否:0  是:1
EnableLog=0

