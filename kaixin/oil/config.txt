# 开心网拉力赛外挂 配置文件 Config.txt

# "#"在行首表示该行是注释行

# 设置每次链接的延时(秒), 默认是5秒
# 如果遇到开心网服务器拒绝你的频繁，请适当加大这个数值
DelayConnect=8

# 设置每次链接的超时(秒)，默认是120秒
HttpTimeout=120

# 设置每次HTTP请求失败后尝试的次数，默认是3次
HttpTryTimes=3

#---------------------代理设置 ---------------------
# 是否启用代理服务器(HTTP代理)(1-启用,0-不启用)
#isProxy=1

# 代理服务器地址
ProxyServer=10.10.9.44

# 代理服务器端口
ProxyPort=808

# 代理服务器用户
ProxyUser=abc

# 代理服务器用户密码
ProxyPass=123

# 代理的域名(一般为空)
ProxyDomain= 




